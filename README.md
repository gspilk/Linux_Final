# Linux_Final

## Project Description
A simple web app (a to-do list) using Node.js or Python/Flask, containerized with Docker, and set up a GitHub Actions pipeline that automatically builds, tests, and deploys the app whenever code is pushed to main.

