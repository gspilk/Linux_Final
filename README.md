# Linux_Final

## Project Description
Build a simple web app (e.g. a to-do list or blog) using Node.js or Python/Flask, containerize it with Docker, and set up a GitHub Actions pipeline that automatically builds, tests, and deploys the app whenever code is pushed to main.